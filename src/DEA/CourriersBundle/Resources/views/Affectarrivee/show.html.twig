{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Affectarrivee</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Dateaff</th>
                <td>{{ entity.dateaff|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Lastaff</th>
                <td>{{ entity.lastaff }}</td>
            </tr>
            <tr>
                <th>Rep</th>
                <td>{{ entity.rep }}</td>
            </tr>
            <tr>
                <th>Daterep</th>
                <td>{{ entity.daterep|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Traitement</th>
                <td>{{ entity.traitement }}</td>
            </tr>
            <tr>
                <th>Urgent</th>
                <td>{{ entity.urgent }}</td>
            </tr>
            <tr>
                <th>Arrivee</th>
                <td>{{ entity.arrivee }}</td>
            </tr>
            <tr>
                <th>Division</th>
                <td>{{ entity.division }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ entity.user }}</td>
            </tr>
            <tr>
                <th>Annotation</th>
                <td>{{ entity.annotation }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('affectarrivee') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('affectarrivee_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
